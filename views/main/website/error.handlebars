<main class="main">
    <div class="page-header text-center">
		<div class="container">
			<div class="row">
                <div class="col-sm-12 col-lg-12">
                    {{#if message}}
                    <div class="alert alert-{{message.alert}}">
                        <h4 class="text-white">{{message.text}}</h4>
                    </div>
                    {{/if}}
                </div>
            </div>
		</div><!-- End .container -->
	</div>
</main>